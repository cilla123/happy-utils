const {
    accAdd,
    accDiv,
    accMul,
    accSub,
} = require('../src/number')

// 加
console.log(accAdd(0.1, 0.2))

// 除
console.log(accDiv(0.3, 0.1))

// 乘
console.log(accMul(0.2, 6))

// 减
console.log(accSub(1, 1.22))

